# Core dependencies (always needed)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0.0
openai>=1.0.0
python-dotenv>=1.0.0
# Needed for "local" Ollama/OpenAI-compatible mode and "api" mode
openai>=1.0.0

# Optional: for Qdrant (production RAG)
qdrant-client>=1.12.0

# Optional: for Redis caching
# redis>=5.0.0

# Optional: for local LLM (requires GPU)
# vllm>=0.6.0
# torch>=2.0.0
# transformers>=4.40.0

# Optional: for embedding (production)
sentence-transformers>=3.0.0
# FlagEmbedding>=1.2.0

# Optional: for fine-tuning
# unsloth>=2024.0
# trl>=0.9.0
# datasets>=2.20.0
# accelerate>=0.34.0
# peft>=0.12.0
# bitsandbytes>=0.43.0
# modal>=0.70.0
